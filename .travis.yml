language: cpp
compiler: g++
before_install:
    - sudo add-apt-repository -y ppa:boost-latest/ppa
    - sudo apt-get update -q
    - sudo apt-get install -y cmake boost1.55
script: make UNIT_TESTS=OFF configure && make
branches:
    only:
        - travis